#include <string>
using namespace std;

int calculatorManager(string, int, char);
char checkEntity(string, int);
char findNextOper(string, int);
char findPrevOper(string, int);
int calculator(int,int,char);
int checkPriority(char, char);